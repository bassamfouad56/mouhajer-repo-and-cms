export const metadata = {
  title: 'Sanity Studio | Mouhajer Design',
  description: 'Content Management Studio',
}

export default function StudioLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
